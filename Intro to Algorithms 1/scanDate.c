#include <stdio.h>

int month, day, year

int main(void)
{
	printf("What is the date? (mm/dd/yyyy): \n);
	scanf("%d/%d/%d, month, day, year);
	print("The date today is %d%.2%.2%, month, day, year);
	return 0;
}